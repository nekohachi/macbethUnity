{
  "name": "macbeth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tablet-first polygon modeling app. v1.0 targets modeling on iPad and Android.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "shot": "node scripts/shot.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "@types/three": "^0.185.4",
    "playwright": "^1.63.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "three": "^0.185.1"
  }
}
