# Opus 作業指示書 #15: S2-3 マスクと裏面マスク

作成: 設計担当。実装担当（Opus）向け。2026-09-09。
`33` が終わってから着手。`28` v3.2 の **S2-3**。

---

## 0. 門の結果と、この指示書の位置

`33` の T5 の門は **通った**。iPad mini 6（25 万四角形のレベル 2）で B6a 17ms /
B6b 19ms。目標 16ms に 1ms 超過だったが、そのあと 1 打ちから `Map` と `Set` を
外して CI で 26.9 → 16.3ms（`33` の末尾）。**wasm への寄り道はしない。**
`28` の表のとおり S2-3（マスク）へ進む。

MovinkPad は 100 万で測られていて門の条件（25 万）と違う。100 万は描画だけで
40ms かかる大きさなので、そもそもブラシの数字を見る所ではない。**25 万で測り
直してもらう**（ユーザー側の宿題。5 章）。

---

## 1. 何を作るか

`05` の「マスキング」から、v1.5 に要る分:

| 入れる | 理由 |
|---|---|
| **ペンで描くマスク**（CTL ラッチ中のストローク） | ZBrush の Ctrl+ドラッグ。彫らない所を決める基本 |
| **消す**（CTL + ALT） | 描き過ぎを戻す |
| **ぼかし / 反転 / 全解除** | 境目をなじませる、逆側を彫る、やり直す。この 3 つが無いとマスクは使い物にならない |
| **裏面マスク** | **これが無いと薄い形が破綻する**（`05`）。ムーブで耳を引くと反対側の耳まで動く |
| **Shift で一時スムース** | `04` の表。ZBrush の Shift と同じ。マスクと同じ「修飾で役割が変わる」仕組みに乗せる |

入れないもの（S2-4 以降）: ポリグループ単位、キャビティ、モデリングの選択面から
マスク（`05` の 19 行目）、マスクの形を残したまま段を跨いで細部を保つこと。

### 決めの要点

- **マスクは頂点ごとの 0〜1。** 0 = 彫れる、1 = 彫れない。ブラシの重みに `(1 - mask)` を掛けるだけ
- **1 つの段にだけ持つ**（`o.mask = { level, values }`）。段を変えたら**移す**（4 章）。
  ZBrush も段ごとに持っているが、v1.5 では 1 段で十分。下へ移して上へ戻すと
  細部が消えることは受け入れる（ぼかしで直る程度）
- **無いときは `null`。** 掛け算も色も要らない。マスクを使わない人に 1 バイトも
  払わせない
- **裏面マスクは既定でオン。** ZBrush の既定はオフだが、タブレットで薄い形を
  触るときに切り忘れて壊すほうが痛い
- **ストロークの役割は押した瞬間に決めて、離すまで変えない。** 途中で CTL を
  切っても、そのストロークはマスクのまま

---

## 2. 順番と理由

| 順 | タスク | 理由 |
|---|---|---|
| T1 | **core にマスク**（塗る / ぼかす / 反転 / 段を移す）と、**ストロークがマスクと裏面を見る** | 形の正しさをテストで固める。UI から切り離す |
| T2 | **データと保存**（`SceneObject.mask`、`.mbz`、段を変えたときの移し替え、トポロジが変わったら捨てる） | ここを後回しにすると、履歴と表示が仮の置き場に依存して作り直しになる |
| T3 | **つなぐ**（CTL でマスクを描く、Shift で一時スムース、履歴、マスクの色） | 触れるようにする |
| T4 | **メニュー**（マスクのボタン、長押しで ぼかし / 反転 / 全解除 / 裏面マスク）と言い訳 | 使えるようにする |
| T5 | **門**: 1 打ちの重さが変わっていないこと、通し確認、実機の確認項目 | 掛け算 1 つと法線 1 回で終わっているか |

**T5 が終わったら報告して止まる。**

---

## T1. core のマスク

### `src/core/mask.ts`（新規）

`src/core/sculpt.ts` と同じ作法。**`Mesh` と `Float32Array` を触るだけ。**
マルチ解像度も履歴も描画も知らない。

```ts
/** マスクを 1 回当てる。values を書き換え、実際に変わった頂点を返す。 */
export function paintMask(
  mesh: Mesh, fp: Footprint, values: Float32Array,
  input: { point, radius, strength, erase: boolean, excludeNearX? },
): Uint32Array;

/** 1 リングの平均へ寄せる。全頂点。 */
export function blurMask(mesh: Mesh, tri: Uint32Array, values: Float32Array): void;

/** 1 - v。全頂点。 */
export function invertMask(values: Float32Array): void;

/** 1 つ上の段へ移す（線形）。下の段のメッシュとその値を渡す。 */
export function maskUp(below: Mesh, values: Float32Array): Float32Array;

/** 1 つ下の段へ移す。上の段の値の先頭 V_below 個をそのまま。 */
export function maskDown(values: Float32Array, belowVertexCount: number): Float32Array;
```

#### 塗る

`applyStroke` の Standard と同じ骨格。`strokeFootprint` で範囲を取り、
`falloff(d / radius) * strength` を **足す**（`erase` なら引く）。0〜1 に切る。
値が変わった頂点だけ返す（履歴と部分描画に渡すので、`applyStroke` と同じ約束）。

1 打ちの濃さは `DAB_DEPTH` と同じ考えで、**打つ間隔（半径の 1/4）と対**にする。
強さ 1 で 1 なぞりすると 1 に届く程度（1 打ち 0.125 × 8 打ち）。既定の強さ 0.67
なら 1 なぞりで 0.67 になり、重ねれば 1 になる。

対称のときの中心線（`33` の T4 の `excludeNearX`）はそのまま効かせる。

#### ぼかす

三角形の辺を使って、隣の平均へ寄せる。`localAverages` と同じ「辺を両向きに足す」
やり方で、**全頂点を 1 回**。読みながら書かない（先に全部読む）。
1 回で 1 リングぶん。ボタンを押すたびに広がる。

#### 段を移す

細分割の出力の並びは **頂点点 `[0, V)` → エッジ点 `[V, V+E)` → 面点 `[V+E, ...)`**
（`subdivide.ts` の 121 行目）。wasm 版も同じ並び（`subdiv-wasm.test.ts` で
1 ビットも違わないことを見ている）。

- **上へ**: 頂点点はそのまま、エッジ点は両端の平均、面点は角の平均。
  `below.edges()` と `below.faceVerts(f)` の並びは `SubdivPlan` が使うものと
  同じなので、**`SubdivPlan` を作らずに済む**（作ると 25 万で数秒かかる）
- **下へ**: 先頭 `V_below` 個を切り出すだけ

Catmull-Clark の重みは使わない。マスクは柔らかい場なので線形で足りる。

### `src/core/sculpt.ts` に足す

`StrokeInput` に 2 つ:

```ts
/** 頂点ごとのマスク（0〜1）。あれば重みに (1 - mask) を掛ける。 */
mask?: Float32Array;
/**
 * 裏面マスク。視線の向き（オブジェクト空間、カメラから面へ）。
 * あれば、法線がこれと同じ向きを向いている（= 裏を向いている）頂点を触らない。
 */
viewDir?: readonly [number, number, number];
```

- **マスク**: `weights[i] *= 1 - mask[v]`。重み 0 になった頂点は動かさないし返さない
  （`applyStroke` の約束どおり）
- **裏面**: `localNormals` は Standard しか作っていない。`viewDir` があれば
  Move と Smooth でも作り、`n · viewDir > 0` の頂点は重み 0。
  **法線は範囲の三角形から作るので、追加の重さは範囲の大きさぶんだけ**
  （メッシュ全体には触らない。`sculpt.ts` の前提を守る）

### テスト（`tests/mask.test.ts`）

- 塗る: 中心がいちばん濃く、半径の外は 0 のまま、1 を超えない、`erase` で戻る、
  変わった頂点だけ返す
- ぼかす: 1 点だけ 1 の場が、1 回で隣へ広がる。0〜1 を出ない。10 回掛けても
  合計（総量）がほぼ保たれる
- 反転: 2 回で元に戻る
- 段を移す: `maskDown(maskUp(m)) === m`（先頭が頂点点なので必ず戻る）。
  上げたエッジ点は両端の平均になっている
- `applyStroke` にマスク 1 を渡すと**1 ミリも動かない**。0.5 なら半分
- 裏面: 球に `viewDir` を渡して当てると、**裏側の頂点は動かない**。渡さなければ
  裏も動く（今までどおり）。Move と Smooth でも同じ
- **裏面マスクの重さ**: `viewDir` の有無で `applyStroke` の時間が 2 倍を超えない
  （範囲の法線 1 回ぶん）

---

## T2. データと保存

### `SceneObject`

```ts
/** マスク（`34`）。無ければ null。1 つの段にだけ持つ。 */
mask: { level: number; values: Float32Array } | null = null;
```

- `markTopologyChanged()` で **捨てる**（段と同じ扱い。頂点の数が変わるので持てない）。
  戻り値に `droppedMask: boolean` を足して、トーストで言う
- `clone()` があれば複製する

### `.mbz`

- `mask/<id>.bin`（f32）と JSON に `maskLevel`。無ければ `null`
- `MBZ_FORMAT_VERSION` は **1 のまま**。無いファイルを無いと読むだけなので、
  古い読み手を壊さない（`06` の「必ず版を入れる」は、壊すときに上げるため）
- `tests/io.test.ts` に往復を 1 つ

### 段を変えたとき（`app.ts` の `goLevel`）

`o.mask` があって `o.mask.level !== 次の段` なら、**1 段ずつ** `maskUp` / `maskDown`
で移す。`levelsOf(o).level(i)` で各段のメッシュが取れる。

- 段を足したとき（`addLevel`）は今の段のまま（新しい段へは行かないなら移さない。
  行くなら `goLevel` が移す）
- 段を捨てたとき（`dropAboveLevel`）: マスクの段が残る段より上なら、残る段へ下ろす
- 焼き込んだとき（`burnDownLevel`）: マスクの段も `at` だけ引く。0 未満になるなら
  捨てる（レベル 0 に持てるが、モデリングでは使わないので）

**段を移すのは履歴に入れない**（段を変えること自体が履歴に入らないのと同じ）。
その代わり、履歴のマスクの差分は**段を憶えている**（T3）。

---

## T3. つなぐ

### ストロークの役割（`src/app/stroke.ts`）

`begin` で決める。**離すまで変えない。**

| 押した瞬間 | 役割 |
|---|---|
| CTL（ラッチ、または PC の Ctrl / ⌘） | **マスクを描く**。ALT も立っていれば**消す** |
| Shift（ラッチ、または PC の Shift） | **スムース**（ブラシの種類は変えない。このストロークだけ） |
| どちらも無し | 今のブラシで彫る（ALT で反転。今までどおり） |

`Live` に `role: "sculpt" | "mask"` と `kind: BrushKind`（Shift のときは `"smooth"`）を持つ。
`stamp` と `hit` は `role` で分岐する:

- `sculpt`: 今までどおり。ただし `StrokeInput` に `mask: o.mask?.values`
  （段が同じときだけ。違うときは T2 の移し替えが済んでいるはずなので同じ）と、
  裏面マスクがオンなら `viewDir` を渡す
- `mask`: `o.mask` が無ければ**その段で作る**（全部 0）。`paintMask` で書く。
  `flush` は `viewport.refreshMask(o, verts)`（下）を呼ぶ。段のデルタは触らない

**`viewDir` の作り方**: `hitLocal` と同じ変換で、カメラの向き（`camera.getWorldDirection`）
をオブジェクト空間へ回す（**平行移動は掛けない**。`matrixWorld` の回転だけ。
スケールが入るなら正規化）。ストロークの間は動かないので `begin` で 1 回作る。

**筆圧**: マスクにも効かせる（半径と強度、`brushAt` そのまま）。

### 履歴（`src/app/history.ts`）

`Entry` に `mask` を足す:

```ts
interface MaskDiff {
  ref: SceneObject;
  /** マスクがあった段。 */
  level: number;
  verts: Uint32Array;
  /** verts.length。 */
  before: Float32Array;
  after: Float32Array;
  /** 全解除など「無かった / 無くなった」の前後。 */
  wasNull: boolean;
  isNull: boolean;
}
```

- 描く: `beginMask` / `trackMask` / `commitPending` を `sculpt` と同じ形で。
  控えるのは **初めて触ったときの値**（同じ頂点を何度なぞっても「ストロークの前」）
- ぼかし / 反転: `verts` = 全部、前後を全部持つ（4 バイト × V。25 万で 1MB。十分軽い）
- 全解除: `isNull = true`、`before` = 全部、`after` = 空
- **当てるとき**（`applyDiff`）: `o.mask` の段が `diff.level` と違えば、まず
  `o.activeLevel = diff.level` にして表示を組み直してから当てる（sculpt の
  差分は段ごとにデルタがあるのでそれで済むが、マスクは 1 段にしか無いので
  **その段へ行く**）。`onMaskUndo(o, verts | null)` で描画を直す

### 描画（`src/app/render/viewport.ts`）

- `ObjectView` に `masked?: MeshPhongMaterial`（`MAT.surf.clone()` に
  `vertexColors: true`）。`applyDisplay` で、**スカルプトモードで `o.mask` が
  あるとき**だけこれを使う（`faded` / `checker` / `heat` と同じ並びに置く。
  優先は `checker` > `heat` > `masked` > `faded` > `MAT.surf`。ヒートは UV の
  確認なので、そのときはマスクを見せなくてよい）
- `surface.geometry` の `color` 属性を、頂点ごとに `1 - 0.65 × mask` の灰色で
  埋める（ZBrush の暗い灰色）。属性は `slots.surfaceSlots` で頂点 → コーナーに
  展開する（`refreshMoved` と同じ表）
- `refreshMask(o, verts)`: 動いた頂点のぶんだけ `color` を書き換える
  （`refreshMoved` と同じ形。`needsUpdate` だけ）
- `refreshMaskAll(o)`: ぼかし / 反転 / 解除 / 段の移し替え / 読み込みのあと
- `rebuildObject` は `o.mask` があれば色も作る

**マスクが `null` になったら `masked` は使わない**（`MAT.surf` に戻る）。
色の属性は残っていてもよい（材質が見ないだけ）。

### ワイヤ・点

触らない。スカルプトではワイヤを出さない（`33` の T4）ので、色を付ける先は面だけ。

---

## T4. メニューと言い訳

### ツール列（`sculptToolColumn`）

「筆」の下、「段」の上に **「マスク」** を 1 つ:

| 操作 | 動き |
|---|---|
| タップ | **CTL ラッチを入れる / 切る**（`state.mods.ctrl`）。修飾クラスタの CTL と同じもの。押されている見た目は `mods.ctrl !== "off"` |
| バッジ | `o.mask` があれば "M" |
| 長押し | 下の 8 方位 |

| 方位 | 項目 | 動き |
|---|---|---|
| N | ぼかし | `blurMask` 1 回。履歴 1 段 |
| E | 反転 | `invertMask`。履歴 1 段 |
| S | 全解除 | `o.mask = null`。履歴 1 段。無ければ何もしない（トースト「マスクはありません」） |
| W | 裏面マスク | `brush.backfaceMask` の入り切り。`sub` に「オン / オフ」。履歴に入れない |

`BrushState` に `backfaceMask: boolean`（既定 **true**）。

アイコンは `ICONS.mask` を 1 つ描く（24×24。半分塗った円など。既存の `sym` の
作法で）。

### 修飾の表示

修飾クラスタの CTL は今でも使える。マスクのボタンとクラスタの CTL は**同じ状態**を
指すので、片方を押せばもう片方も押された見た目になる（`syncModButtons` と
`renderToolColumn` の両方を呼ぶ）。

### 言い訳（`strokeHint`）

- CTL が立っているとき: 「マスクを描いています（ALT で消す）」
- Shift が立っているとき: 「スムース（Shift のあいだだけ）」
- 「段を足してから」などは今までどおり。**マスクを描くのも段が要る**
  （レベル 0 ではモデリングと同じ道になるので、v1.5 では描かせない）

### HUD

`hud.ts` のスカルプトの行に「マスク あり / なし」は**要らない**。バッジで足りる。

---

## T5. 門

### 数字

`?bench=1` の B6a を **CI で** 測り直す。マスクあり（`mask` を全部 0.3 で渡す）と
裏面マスクあり（`viewDir` を渡す）を足して、**どちらも B6a の 1.3 倍を超えない**こと。
ベンチの行にはしない（実機で見たいのは B6a のままなので、行を増やすと読みにくい）。
テストで見る（`tests/mask.test.ts` の重さの項目で足りるなら、そこで）。

### 通し確認（`scripts/smoke.mjs`）

`33` の項目の隣に足す。**終わったら必ずモードとマスクと修飾を戻す**（`33` で
3 回引っかかった。CTL ラッチを切り忘れると、後ろのモデリングの項目が全部
「選択から除外」になる）。

1. 段を 2 つ足し、CTL ラッチを入れてなぞる → `o.mask` ができて、なぞった所が
   0 より大きい。**材質が `masked`** で、`color` 属性がある
2. ラッチを切って同じ所を彫る → **マスクの濃い頂点は動かない**（動いた頂点の
   集合とマスク 1 の集合が交わらない）
3. 反転 → 値が `1 - v`。ぼかし → 分散が減る。全解除 → `null` で材質が `MAT.surf`
4. 取り消しを 3 回 → 順に戻る。やり直し → 順に進む。**履歴の段の数**が合う
5. 段を 1 つ下げる → マスクが残っていて頂点数が合う。上げる → 同じ
6. `.mbz` に書いて読む → マスクが戻る（段も）
7. Shift ラッチでなぞる → ブラシの種類は変わらないまま、スムースが掛かっている
   （ばらつきが減る）。離すと `state.brush.kind` は元のまま
8. 裏面マスク: 球の手前を Move で引く → 裏側の頂点が動かない。切ると動く
9. モデリングでトポロジを変える → マスクが消えてトーストに出る

PNG: `docs/img/34-mask.png`（マスクを描いた球。暗い所が見える）と
`docs/img/34-mask-menu.png`（長押しメニュー）。`scripts/shot.mjs` に足す。

### 実機で見てもらうこと（報告に書く）

1. CTL を入れてなぞると暗くなる。ALT で消せる
2. 暗い所は彫れない。ぼかしで境目がなじむ
3. **ムーブで耳を引いても反対側が動かない**（裏面マスク）。切ると動く
4. `?bench=1&size=25` の B6a（MovinkPad。門の条件で測り直し）

---

## 決めてあること

- マスクは頂点ごとの 0〜1、1 つの段にだけ持つ。段を変えたら線形で移す
- 無いときは `null`。掛け算も色も払わない
- 裏面マスクは既定でオン。法線は範囲の三角形から作る（メッシュ全体に触らない）
- ストロークの役割は押した瞬間に決めて離すまで変えない
- `src/core/mask.ts` は `Mesh` と `Float32Array` を触るだけ

## やらないこと

- ポリグループ・キャビティ・選択面からのマスク（S2-4 以降）
- マスクの段ごとの保持（v2）
- マスクの表示色の調整 UI
- 残りのブラシ、レイヤー、アルファ（S2-4）
- wasm（S2-5）

---

## 門の測り直し（2026-09-09。ユーザー提供）

MovinkPad 14 を**門の条件**（`?bench=1&size=25`）で測り直してもらった。

| | mini 6（25 万） | MovinkPad 14（25 万） |
|---|---|---|
| B6a ストローク 1 コマ・上の段 | 17ms | **15.7ms**（目標 16 ✓） |
| B6b 1 つ下の段 | 19ms | 35.7ms |
| B4a 描画 1 フレーム | 9ms | 36.5ms |
| B1b-wasm 細分割 | 68ms | 188.9ms |
| B2b デルタの取り直し | 57ms | 86ms |

**B6a は両方 16ms に入った。** `33` の T5 の表の 1 行目（「両方 16ms 以内」）で、
S2-3 へ進むのが正しい。ユーザーの手触りも「引っかかりは感じない」。

B6b（1 つ下の段で彫る）は越えているが、これは表の 2 行目のとおり**逃げてよい**
（`03` の売りである「ハイを見ながらローを直す」は S2-5）。

B4a が MovinkPad で 36.5ms なのは目を引くが、これは 49 万三角形を出したときの
数字で、**画素比 1.75 の広い画面**ぶん mini（9ms）より重い。ふだんの大きさなら
問題にならない。S2-5 で見る。

---

## 実装の報告（Opus、2026-09-09）

### 先に直したもの: 筆の円が楕円に見えた（ユーザー報告）

**輪を「当たった点の法線に垂直な平面」に置いていた。** 面が傾いている所では
必ず楕円に見える。輪は**太さを読み取るためのもの**なので、面の傾きより
「どこでも同じ丸」を優先して、**カメラに正対**させた（ZBrush も Nomad も同じ）。

ついでに `nearestNormal` が要らなくなった。あれは輪を出すたびに**全頂点**を
なめていた（23 万頂点で 1 コマごと）。ホバーは指が動くたびに起きるので、
ここは静かに効いていたはず。

通し確認に「筆の円が画面で丸い」を足した。中央と、球のふち（面がいちばん
傾いている所）の 2 か所で、画面に落とした輪の**縦横比**を見る。どちらも 1.000。
輪の平面をわざと傾けると 0.858 になって落ちる（確かめた）。

比を「中心からの距離の最小 / 最大」で測ると、輪の閉じ点が 1 点目と重なって
重心がずれ、真円でも 0.96 に出る。**囲む箱の縦横**で見ること。

### T1: core のマスク

`src/core/mask.ts`（新規）。`sculpt.ts` と同じ作法で、**`Float32Array` を
書き換えるだけ**。

| 関数 | 中身 |
|---|---|
| `paintMask` | 1 打ち。`MASK_DAB = 0.125`（打つ間隔の 1/4 と対。**強さ 1 で 1 なぞりするとちょうど塗り切る**）。0〜1 に切って、**変わった頂点だけ**返す |
| `blurMask` | 三角形の辺で 1 リングぶん平均へ。自分も 1 票入れる（入れないと 1 回で丸くなりすぎる）。`Mesh` は要らず `tri` だけ |
| `invertMask` | `1 - v` |
| `maskUp` / `maskDown` | 段を移す。**`SubdivPlan` を作らない**（25 万で数秒かかる）。並びが 頂点点 → エッジ点 → 面点 と決まっているので、`below.edges()` と `faceVerts` だけで足りる |
| `maskIsEmpty` | 全部 0 か。app 側で `null` に落とすため |

`StrokeInput` に 2 つ足した。

- `mask?: Float32Array` — 重みに `(1 - mask)` を掛ける。短ければ足りない分は 0
- `viewDir?` — 裏面マスク。**法線がこれと同じ側を向いている頂点を触らない**

法線は `localNormals` を **Standard 以外でも**作るようにした（前は Standard だけ）。
ただし**範囲の三角形からしか作らない**ので、メッシュ全体には触らない。
テストで「マスクと裏面マスクを渡しても当てる時間が 2 倍を超えない」を見ている。

`tests/mask.test.ts` に 22 項目。**core 302 テスト**（280 → 302）。

見ているもののうち大事な 2 つ:

- **マスク 1 なら 1 ミリも動かない**（Standard / Move / Smooth の 3 つとも）。
  0.5 なら比がちょうど 0.5
- **裏面マスクで背を向けた頂点が動かない**。渡さなければ今までどおり裏も動く

### T2: データと保存（2026-09-10）

**`35` の割り込み（スムースの隣の数え方）を先に済ませてから着手した。**

#### 持ち方

`SceneObject.mask: { level, values } | null`。**0 で埋めた配列は持たない。**
全部 0 になったら `null` へ落とす（`maskIsEmpty`）。

`markTopologyChanged()` で捨てる。戻り値に `droppedMask` を足して、
トーストの 3 か所（押し出し・結合・トポロジ編集）で「マスクを破棄」と言う。

#### 段の移し替え（`levels.ts` の `moveMaskTo`）

`goLevel` / `addLevel` / `dropAboveLevel` / `burnDownLevel` の 4 か所から呼ぶ。

| 場面 | すること |
|---|---|
| 段を移る | 1 段ずつ `maskUp` / `maskDown`。`SubdivPlan` は作らない |
| **レベル 0 へ寄る** | **何もしない。** 0 では彫らないので、下ろして戻すと細部が消えるだけ損 |
| 段を足す | 新しい段へ連れていく |
| 上を捨てる | **生きたスタックを捨てる前に**下ろす（下ろすのに各段のメッシュが要る） |
| 焼き込む | 番号を振り直すだけ（段ごとの頂点は変わらない）。新しいレベル 0 以下は捨てる |

#### 履歴

**全複製の控えにマスクを入れた**（`ObjectSnapshot.mask`）。段の操作は全複製で
控えるので、ここを入れないと取り消したときにマスクだけ段とずれる。
`values` は `paintMask` がその場で書き換えるので、**控えるときも戻すときも複製する**。

#### `.mbz`

`mask/<id>.bin`（f32）と JSON の `maskLevel`。無ければファイルも作らない。
**`MBZ_FORMAT_VERSION` は 1 のまま**（無いものを無いと読むだけなので、古い
読み手を壊さない）。

#### 見ていること

`tests/maskLevels.test.ts` を新設（7 項目）と、`tests/io.test.ts` に 3 項目。
**core 319 テスト**（309 → 319）。

- 上げ下げで頂点の数がその段に合う。全部同じ値なら上げても同じ値のまま
- **上げて下げると下の段の値は元どおり**
- レベル 0 へは移さない。段より上を指してもいちばん上で止まる
- 全部 0 なら持たない
- `.mbz` の往復。マスクが無ければ `mask/` のファイルを作らない
- トポロジを変えると捨て、元々無ければ「捨てた」と言わない

通し確認に 1 つ。段を上げ下げしてマスクが付いて回り、レベル 0 へ寄っても
消えず、`.mbz` に残り、トポロジを変えると消えること。

### T3: つなぐ（2026-09-10）

#### ストロークの役割

**押した瞬間に決めて、離すまで変えない。** `Live` に `role` / `kind` / `erase` /
`viewDir` を持つ。途中で CTL を切っても、そのストロークはマスクのまま
（役割が入れ替わると、履歴の 1 段に彫りと塗りが混ざる）。

| 押した瞬間 | 役割 |
|---|---|
| CTL（ラッチ or Ctrl / ⌘） | マスクを描く。ALT も立っていれば消す |
| SHF（ラッチ or Shift） | **このストロークだけ**スムース。`brush.kind` は変えない |
| どちらも無し | 今のブラシ（ALT で反転） |

**`live.kind` で分岐すること。** ムーブの特別扱いを `state.brush.kind` で見ていた
ままだと、SHF の一時スムース中にムーブの道へ入ってしまう。

#### 裏面マスク

`BrushState.backfaceMask`（**既定オン**）。押したときに視線をオブジェクト空間へ
1 回だけ移して `viewDir` に持つ（ストロークの間はカメラが動かない）。

#### マスクを彫るときに渡す

**段が合っているときだけ**渡す（`maskFor`）。段が違うマスクを渡すと頂点の番号が
ずれて、見当違いの所が固くなる。

#### 履歴

`Entry` に `mask` を足した。`beginMask` / `trackMask` / `commitPending`。
控えるのは**初めて触ったときの値**。`wasNull` / `isNull` で「無かった / 無く
なった」を持つので、取り消すと `null` に戻る。

ぼかし・反転・全解除むけに `pushMaskChange`（全頂点を前後で持つ）も用意した。
25 万四角形でも 2MB ほどで、段の全複製よりずっと軽い。T4 で使う。

`applyDiff` はマスクの段へ行ってから当てる。`onMaskUndo` で表示を作り直す。

#### 描画

`ObjectView.masked` は **`MAT.surf` の複製**に頂点色を立てたもの。色は
`1 - 0.65 × mask` で**素の色に掛かる**ので、陰影が残って形が読める
（`heatMaterial` のように光を無視すると、マスク中だけ形が見えなくなる）。

**`colorKind` を足した。** ヒート・極・マスクが同じ `color` 属性を取り合うので、
違う種類が入ったまま部分更新すると、マスクの上に価数の色が残る。
塗っている最中は `refreshMask`（動いた頂点だけ）、丸ごと変わるときは
`refreshMaskAll`。

#### 見ていること

通し確認に 1 つ。CTL を入れて 6 回なぞり、切って同じ所を彫る。

| 見たもの | 結果 |
|---|---|
| マスクができる | 段 2・最大 1.00・履歴の種類 `mask` |
| 暗くなる | 頂点色オン・いちばん暗い所 0.35（= `1 - 0.65`） |
| **塗り切った所は彫れない** | 動いた 152 頂点のうち、**塗り切った 98 頂点は 0 個** |
| SHF は一時的 | 離したあとも `brush.kind` は `standard` のまま |
| 取り消し | マスクが `null` に戻る |

書きながら 2 度、**テストのほうが間違っていた**。

1. 16px のなぞりでマスクが 0.17 までしか乗らなかった。1 打ちの濃さは筆の半径に
   対して決まっているので、短すぎるなぞりでは 1 打ちしか乗らない。**実装は正しい**
2. 「マスク 0.6 超が動いた」で落ちた。0.6〜0.99 は**効きが弱くなるだけで動くのが
   正しい**。見るべきは 1.0 の頂点。さらに「濃い所の平均 vs 薄い所の平均」も
   足しかけたが、濃い所は筆の真ん中・薄い所は球の裏まで含むので**比べても意味が
   ない**。効き目の比は単体テスト（「0.5 ならちょうど半分」）で見ている

### T4: メニューと言い訳（2026-09-10）

ツール列の「筆」と「段」のあいだに**マスク**を 1 つ。

| 操作 | 動き |
|---|---|
| タップ | **修飾クラスタの CTL そのもの**を入り切り。別の状態にすると「ボタンは光っているのに CTL は消えている」が起きる |
| バッジ | マスクがあれば "M" |
| 長押し N | ぼかす |
| 長押し E | 反転 |
| 長押し S | 全解除（無ければ「マスクはありません」） |
| 長押し W | 裏面マスクの入り切り（履歴に入れない） |

ぼかし・反転・全解除は `pushMaskChange` で履歴 1 段。前後を全頂点ぶん持つが、
25 万四角形でも 2MB ほどで段の全複製よりずっと軽い。

**ぼかしはマスクの段の三角形で数える。** 見ている段が違うと頂点の番号がずれて、
見当違いの所がなじむ。ふつうは同じなので `view.tri` をそのまま使う。

![マスク](img/34-mask.png)

![マスクの長押しメニュー](img/34-t4-mask-menu.png)

#### 端でも西が選べるか確かめた

マスクのボタンは**左端**にあるので、長押しの輪が画面からはみ出して W の項目が
切れる。`33` で「段が足せない」を踏んだのと同じ形なので、**中身を直に叩かず
輪から選べるか**を通し確認に入れた。長押しして西へ 140px 引き、裏面マスクが
実際に切り替わることを見ている。

輪は端で切れても「引いた向き」で選ぶ決まり（`radial.ts`）なので通る。
ラベルが見切れるだけで、操作はできる。

#### ついでに直した

`applyDiff` のマスクの枝で `Math.max(...Array.from(verts))` を書いていた。
25 万四角形のマスクは頂点が数十万あるので、**引数に展開するとスタックが溢れる**
（`30` の T1 で踏んだのと同じ）。ループに直した。

#### 見ていること

通し確認に 1 つ。ボタンが出る / タップで CTL が入りクラスタも光る / もう一度で
切れる / マスクが無いのにぼかすと言い訳が出る / ぼかしでばらつきが 0.250 → 0.077 /
反転 / 全解除で素の材質に戻る / 取り消しで戻る / 端でも西へ引いて裏面を選べる。

既存の「スカルプトのツール列」の項目も、マスクが入ったぶん直した。

### T5 への引き継ぎ（設計担当、2026-09-10）

T5 の通し確認 9 項目のうち、**T2〜T4 でもう入っているもの**を先に整理する。
重ねて書かないこと。

| # | 中身 | 状態 |
|---|---|---|
| 1 | CTL でなぞるとマスクができ、材質が `masked` | **済**（T3 の「CTL でマスクを描き…」） |
| 2 | 塗り切った所は彫れない | **済**（同上。塗り切り 98 頂点が 0 個動く） |
| 3 | 反転 / ぼかし / 全解除 | **済**（T4 の「マスクのボタン…」） |
| 4 | 取り消し 3 回で順に戻る、やり直しで進む、段の数が合う | **半分**。取り消しは見ている。**やり直しと履歴の段の数は見ていない** |
| 5 | 段の上げ下げでマスクが付いて回る | **済**（T2） |
| 6 | `.mbz` の往復 | **済**（T2） |
| 7 | SHF の一時スムース | **済**（T3。種類が変わらないことだけ。**ばらつきが減ることは見ていない**） |
| 8 | 裏面マスク: ムーブで手前を引いて裏が動かない、切ると動く | **未**。単体テストにはある（`tests/mask.test.ts`）が、app の道（`viewDir` を作って渡す所）は通していない |
| 9 | トポロジを変えるとマスクが消えてトーストに出る | **半分**。消えることは T2 で見ている。**トーストの文言は見ていない** |

**T5 で足すのは 4 の後半・7 の後半・8・9 の後半。** 1 つの項目にまとめてよい。

PNG は 2 枚とも撮ってある（`34-mask.png` / `34-t4-mask-menu.png`。指示書の
`34-mask-menu.png` とは名前が違うが、撮り直さなくてよい）。

#### 数字

指示書は「B6a の 1.3 倍を超えない」と書いたが、B6a はマスクを渡さない。
測るのは **`33` の末尾と同じ 1 打ちの内訳**（CI・25 万四角形・1.66 万頂点）で、
「範囲を拾って動かす」を **マスク無し / マスクあり（全部 0.3）/ マスク + 裏面
（`viewDir` あり）** の 3 通り、各 3 回の中央値。**裏面ありが無しの 1.3 倍以内**。
単体テスト（「2 倍を超えない」）は緩いので、ここで実数を出す。
ベンチの行は増やさない。

#### 報告に書くこと

- 上の 3 通りの数字
- core と通し確認の件数
- 実機で見てもらうこと（下の 4 つ）。**`36` の分と一緒に見てもらう**ので、
  `37` の表に足す形でよい

1. CTL を入れてなぞると暗くなる。ALT で消せる
2. 暗い所は彫れない。ぼかしで境目がなじむ
3. **ムーブで耳を引いても反対側が動かない**（裏面マスク）。切ると動く
4. `?bench=1&size=25` の B6a（MovinkPad）。マスクを入れても変わらないはず

### T5: 門（2026-09-10）

引き継ぎの表のとおり、**残りの 4 つ**を通し確認 1 項目にまとめた。

| 見たもの | 結果 |
|---|---|
| マスク 2 本で履歴 2 段。取り消しで消え、**やり直しで戻る** | 済 |
| **SHF の一時スムースでばらつきが減る** | 5.26e-5 → 1.63e-5（3 分の 1）。種類は `standard` のまま |
| **裏面マスクを app の道で通す**（`viewDir` を作る所） | オン: 手前 3035・**奥 0** / オフ: 奥 2059 |
| **破棄の言葉** | 「面を押し出し — 1 面 · **マスクを破棄** · 上位レベル 2 とレイヤー 0 を破棄」 |

**core 319 テスト、通し確認は `dist` と `app/` の両方で緑。**

#### 書きながら 3 回、テストのほうが間違っていた

1. **でこぼこ具合を座標のばらつきで測った。** 球そのものの形に呑まれて
   3.26e-1 → 3.26e-1 と動かなかった。**原点からの距離のばらつき**に替えた。
   素の球は 0、彫ると増え、均すと減る
2. **筆が既定の太さ（半径 0.23）のままだった。** 半径 1 の球の裏は 2 離れて
   いるので、裏面マスクの有無にかかわらず奥は 0。**球ぜんぶが入る太さ**にした
3. **カメラが斜めのまま「奥 = オブジェクトの -Z」と決めていた。** 裏面マスクは
   効いているのに奥 500 が出た。**カメラを「前」に固定**した

#### 数字（CI・25 万四角形・1.66 万頂点、3 回の中央値）

1 打ちの「範囲を拾って動かす」ところ。マスクは全部 0.3、裏面は `viewDir` あり。

| ブラシ | 素 | マスク | マスク + 裏面 | 裏面の倍率 |
|---|---|---|---|---|
| スタンダード | 5.54ms | 5.56ms | 6.64ms | **1.20×** |
| ムーブ | 4.12ms | — | 5.82ms | **1.41×** |
| スムース | 10.52ms | — | 8.64ms | 揺れの中（8.0〜10.6ms） |

**マスクは只**（1.00×）。掛け算 1 つなので当然。

**裏面マスクはムーブで 1.41 倍になり、門の「1.3 倍以内」を越える。**
理由ははっきりしている。スタンダードは**元から法線を作る**ので、増えるのは
内積のループだけ（1.20×）。ムーブとスムースは法線を作らないので、裏面マスクの
ために**新しく作る**ぶんが丸ごと乗る。

**越えているが、そのままでよいと考える。** 倍率ではなく実数で見ると、
裏面マスク込みのムーブは **5.82ms** で、**素のスタンダード（5.54ms）とほぼ同じ**。
いちばん軽いブラシに法線 1 回ぶんが乗って、いちばん普通のブラシと同じ重さに
なっただけ。門の「1.3 倍」は、いちばん軽いものを基準にすると厳しすぎる形だった。

必要なら法線の正規化を飛ばせる（符号しか見ないので）。ただし重いのは三角形を
なめる足し込みのほうで、取れるのは 2 割ほど。**S2-4 で他のブラシを足すときに
まとめて見るのがよい**（法線を使うブラシが増えると、共通で 1 回作る形になる）。

#### 実機で見てもらうこと

`37` の表に入れてある。**`36` の分と一緒に 1 回で。**
